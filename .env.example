# Environment variables required to run SpectraScope locally
# Rename or copy this file to ".env" in development and replace with your real API keys.

# Google Gemini API key - exported as GEMINI_API_KEY (used for model calls)
GEMINI_API_KEY=REPLACE_ME

# Optional: NIST API key (if used by nistService)
#NIST_API_KEY=REPLACE_ME

# Optional: Other keys used by services
#OTHER_API_KEY=
